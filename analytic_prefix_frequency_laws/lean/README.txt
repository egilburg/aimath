Report-specific Lean supplement for "Analytic laws for recognizable prefix frequencies", revision r1.

`SierpinskiFormal/` is the transitive project-local import closure of `SourceFullAtomicLaw.lean` at verified private source commit 73edf1d96438cde89dbac16c70106003b35d2f06. Unrelated mortality, sofic, aggregate, and audit targets are excluded.

Toolchain and mathlib are pinned. From this directory run:

    lake build ReportProof
    lake env lean ReportProof.lean
