Current verification: manuscript/package 2026-09-10-r3.
Lean 4.33.1, compiler commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6.
All dependency Git HEADs were checked against lean/lake-manifest.json; see ENVIRONMENT.json.

This managed environment required an LD_PRELOAD shim that redirects readlink of the current process /proc/<pid>/exe to /proc/self/exe so Lean can locate its installation. The unchanged C source is runtime-self-exe.c. Compile only if needed: cc -shared -fPIC runtime-self-exe.c -ldl -o runtime-self-exe.so; set LD_PRELOAD to that shared library. This is an executable-location adaptation, not a compiler or kernel modification. A normal Lean installation needs no shim.

Before the final check, all local project build outputs were moved outside the project. bash lean/verify.sh ran with LAKE_ARTIFACT_CACHE=false. The resulting build compiled every one of the 25 local mathematical modules; public dependency artifacts were reused at their verified pins. Fresh build, 20 source-statement checks and endpoint axiom audit all exited zero. Logs are included.
